








//Optimal Approach

class Solution{
    static int countFactors(int n) {
        int cnt=0;
        for(int i=1;i<=(int)Math.sqrt(n);i++)
          if(n%i==0)
          {
              if(i==(n/i))
                cnt++;
              else
                cnt+=2;
          }
      
        return cnt;
    }
}
