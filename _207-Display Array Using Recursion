class Solution{
   public static  void displayArray(int []arr,int idx){  // idx will be passed as 0
      if(idx==arr.length)
	return;
	     
      System.out.println(arr[idx]);
      displayArray(arr,idx+1);
   }
}
