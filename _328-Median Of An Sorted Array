class Solution {
    public static float findMedian(int[] arr) {
        int n = arr.length;
        if ((n % 2) == 0) {
            return (float) (arr[(n - 1) / 2] + arr[n / 2]) / 2.0F;
        } else {
            return (float) arr[n / 2];
        }
    }
}
