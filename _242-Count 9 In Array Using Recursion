//Using Recursion

class Solution{
  public int arrayCount9(int[] arr) {
    if(arr.length==0){
      return 0;
    }
    return arr[0]==9?1+arrayCount9(Arrays.copyOfRange(arr, 1, arr.length)):arrayCount9(Arrays.copyOfRange(arr, 1, arr.length));
  }
}
